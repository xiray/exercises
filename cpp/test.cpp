#include<bits/stdc++.h>
using namespace std;
int main() {
  int n;
  cin>>n;
  int a,b,c;
  int max=sqrt(n);
  for(a=0;a<=max;a++) {
    int a2=a*a;
    for(b=0;b<=max;b++) {
      int b2=b*b;
      for(c=0;c<=max;c++) {
        int c2=c*c;
        int d=sqrt(n-a2-b2-c2);
        
        if(a2+b2+c2+=n) int num=a+b+c+
      }
    }
  }
}